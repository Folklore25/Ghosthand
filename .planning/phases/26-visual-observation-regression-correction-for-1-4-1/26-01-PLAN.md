---
phase: 26-visual-observation-regression-correction-for-1-4-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/26-visual-observation-regression-correction-for-1-4-1/26-01-DIAGNOSIS.md
  - app/src/test/java/com/folklore25/ghosthand/routes/read/ReadScreenshotRouteHandlersTest.kt
  - app/src/test/java/com/folklore25/ghosthand/interaction/execution/ScreenshotDispatchResultTruthTest.kt
  - app/src/test/java/com/folklore25/ghosthand/integration/projection/MediaProjectionProviderTest.kt
autonomous: true
requirements:
  - VIS-03
  - VIS-05
  - VIS-06
must_haves:
  truths:
    - "The current regression is proven end to end before any runtime fix lands."
    - "Empty-image success and full-resolution failure are locked in with targeted failing assertions rather than guessed from manual inspection alone."
    - "This plan stays diagnosis-only and does not change screenshot, preview, or capability behavior."
  artifacts:
    - path: ".planning/phases/26-visual-observation-regression-correction-for-1-4-1/26-01-DIAGNOSIS.md"
      provides: "Exact failing-path map from /screenshot route contract to backend capture behavior"
    - path: "app/src/test/java/com/folklore25/ghosthand/routes/read/ReadScreenshotRouteHandlersTest.kt"
      provides: "Route-level failing regression coverage for empty-image success and default full-resolution expectations"
    - path: "app/src/test/java/com/folklore25/ghosthand/interaction/execution/ScreenshotDispatchResultTruthTest.kt"
      provides: "Shared screenshot truth assertions for blank bytes, zero dimensions, and usable-image expectations"
  key_links:
    - from: "ReadScreenshotRouteHandlers"
      to: "ScreenshotDispatchResult"
      via: "success or failure serialization"
      pattern: "available.*base64|data:image/png;base64,"
    - from: "GhosthandScreenshotAccess"
      to: "MediaProjectionProvider and GhostCoreAccessibilityService"
      via: "backend selection"
      pattern: "first backend with available=true versus first backend with usable bytes"
---

<objective>
Trace the screenshot regression end to end and lock the failing path into focused regression artifacts before any corrective runtime change lands.
</objective>

<context>
Keep this plan strictly bounded to diagnosis and failing-path lock-in per the Phase 26 context and research. Do not implement screenshot fixes, preview policy changes, capability-plane work, action-evidence changes, or contract polish in this plan.
</context>

<tasks>
<task type="auto">
  <name>Task 1: Write the failing-path diagnosis from route contract through both capture backends</name>
  <files>.planning/phases/26-visual-observation-regression-correction-for-1-4-1/26-01-DIAGNOSIS.md</files>
  <action>Trace `/screenshot` from `ReadScreenshotRouteHandlers.kt` through `GhosthandScreenshotAccess.kt`, `MediaProjectionProvider.kt`, and `GhostCoreAccessibilityService.kt`, then write a short diagnosis artifact that states the exact success-with-empty-image path, the exact full-resolution failure hypothesis, whether invalid requested dimensions can produce empty or unusable output, and which truths are backend truth versus route serialization truth. Keep the artifact concrete and implementation-guiding, not philosophical.</action>
  <verify>
    <automated>rg -n "empty-image success|full-resolution|route serialization|MediaProjection|AccessibilityService|invalid requested dimensions" .planning/phases/26-visual-observation-regression-correction-for-1-4-1/26-01-DIAGNOSIS.md</automated>
  </verify>
  <done>The diagnosis artifact names the precise broken path and the exact conditions the later fix plans must correct.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add failing regression tests that lock the broken screenshot truth boundary</name>
  <files>app/src/test/java/com/folklore25/ghosthand/routes/read/ReadScreenshotRouteHandlersTest.kt, app/src/test/java/com/folklore25/ghosthand/interaction/execution/ScreenshotDispatchResultTruthTest.kt, app/src/test/java/com/folklore25/ghosthand/integration/projection/MediaProjectionProviderTest.kt</files>
  <behavior>
    - Test 1: a screenshot result with `available=true` but blank or missing image bytes must not serialize as successful screenshot data.
    - Test 2: default `/screenshot` behavior must preserve full-resolution capture intent instead of silently forcing degraded tiny output.
    - Test 3: projection capture must prove a real frame or fail specifically rather than passing through an empty-image success path.
  </behavior>
  <action>Write focused failing tests that reproduce the currently regressed truth boundary described in the diagnosis artifact. Keep the assertions narrow: blank base64, zero-byte image output, zero or invalid returned dimensions, and immediate projection-frame acquisition failure. Do not modify production code in this plan; the goal is to create a reliable failing lock-in for the next plan.</action>
  <verify>
    <automated>bash -lc '! ./gradlew :app:testDebugUnitTest --tests "*ReadScreenshotRouteHandlersTest" --tests "*ScreenshotDispatchResultTruthTest" --tests "*MediaProjectionProviderTest"'</automated>
    <manual>Run `./gradlew :app:testDebugUnitTest --tests "*ReadScreenshotRouteHandlersTest" --tests "*ScreenshotDispatchResultTruthTest" --tests "*MediaProjectionProviderTest"` and record the expected failing assertions in `26-01-DIAGNOSIS.md`. This task passes only when the failures are intentional, screenshot-specific, and match the diagnosis artifact.</manual>
  </verify>
  <done>The targeted screenshot regression tests fail for the known broken reasons, and no runtime screenshot behavior has been changed yet.</done>
</task>
</tasks>

<verification>
The only acceptable outcome for this plan is a concrete diagnosis plus intentionally failing screenshot regression tests recorded in the diagnosis artifact. The executor must not treat those red tests as a phase failure, but any unrelated red test or any production screenshot fix in this wave is a failure.
</verification>

<success_criteria>
The failing path is documented, reproducible by targeted automated tests, and bounded tightly enough that Plan 26-02 can fix it without reopening wider 1.4.1 scope.
</success_criteria>

<output>
After completion, create `.planning/phases/26-visual-observation-regression-correction-for-1-4-1/26-01-SUMMARY.md`.
</output>
