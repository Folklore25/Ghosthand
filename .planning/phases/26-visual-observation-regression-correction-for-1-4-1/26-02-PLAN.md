---
phase: 26-visual-observation-regression-correction-for-1-4-1
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - app/src/main/java/com/folklore25/ghosthand/interaction/execution/GhosthandScreenshotAccess.kt
  - app/src/main/java/com/folklore25/ghosthand/interaction/execution/ScreenshotDispatchResult.kt
  - app/src/main/java/com/folklore25/ghosthand/integration/projection/MediaProjectionProvider.kt
  - app/src/main/java/com/folklore25/ghosthand/service/accessibility/GhostCoreAccessibilityService.kt
  - app/src/main/java/com/folklore25/ghosthand/routes/read/ReadScreenshotRouteHandlers.kt
  - app/src/test/java/com/folklore25/ghosthand/routes/read/ReadScreenshotRouteHandlersTest.kt
  - app/src/test/java/com/folklore25/ghosthand/interaction/execution/ScreenshotDispatchResultTruthTest.kt
  - app/src/test/java/com/folklore25/ghosthand/integration/projection/MediaProjectionProviderTest.kt
autonomous: true
requirements:
  - VIS-01
  - VIS-03
  - VIS-05
  - VIS-06
must_haves:
  truths:
    - "Normal /screenshot requests return truthful full-resolution capture when the supported path can produce it."
    - "Ghosthand never reports screenshot success when returned bytes, returned dimensions, or encoded payload are empty."
    - "Backend selection prefers a validated real image over the first backend that merely reports availability."
  artifacts:
    - path: "app/src/main/java/com/folklore25/ghosthand/interaction/execution/ScreenshotDispatchResult.kt"
      provides: "Canonical screenshot-result truth helpers or validation fields"
    - path: "app/src/main/java/com/folklore25/ghosthand/routes/read/ReadScreenshotRouteHandlers.kt"
      provides: "/screenshot success or failure shaping that refuses empty-image success"
    - path: "app/src/main/java/com/folklore25/ghosthand/integration/projection/MediaProjectionProvider.kt"
      provides: "Full-resolution projection capture that waits for a real frame instead of racing the first image"
  key_links:
    - from: "GhosthandScreenshotAccess"
      to: "ScreenshotDispatchResult validation"
      via: "usable-image selection"
      pattern: "hasUsableImage|encodedBytes|returnedWidth|returnedHeight"
    - from: "ReadScreenshotRouteHandlers"
      to: "validated screenshot result"
      via: "route response mapping"
      pattern: "success only when image bytes and dimensions are real"
---

<objective>
Restore truthful full-resolution screenshot capture in the supported case and remove the empty-image success path from backend selection through `/screenshot` serialization.
</objective>

<context>
Implement only the bounded screenshot-correction work proven by Plan 26-01. Per the Phase 26 locked decisions, do not fix this by silently forcing tiny degraded images, and do not broaden into preview policy, action evidence, capability redesign, or unrelated 1.4.1 work.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add one canonical screenshot-result validation layer and use it for backend selection</name>
  <files>app/src/main/java/com/folklore25/ghosthand/interaction/execution/ScreenshotDispatchResult.kt, app/src/main/java/com/folklore25/ghosthand/interaction/execution/GhosthandScreenshotAccess.kt, app/src/test/java/com/folklore25/ghosthand/interaction/execution/ScreenshotDispatchResultTruthTest.kt</files>
  <behavior>
    - Test 1: usable screenshot truth requires positive returned dimensions and non-empty encoded image bytes, not `available=true` alone.
    - Test 2: backend selection returns the first validated image result, not the first nominally available result with empty payload.
  </behavior>
  <action>Introduce a single screenshot-result truth layer on or beside `ScreenshotDispatchResult` that answers whether a result has a usable image, what dimensions were actually returned, and which empty or invalid conditions should stop success. Update `GhosthandScreenshotAccess` to select the first backend with validated image bytes and dimensions instead of the first backend with `available=true`.</action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "*ScreenshotDispatchResultTruthTest"</automated>
  </verify>
  <done>Screenshot result truth is canonicalized in one place and backend selection is grounded on validated image output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restore truthful full-resolution capture and reject empty-image success in both backends and the route</name>
  <files>app/src/main/java/com/folklore25/ghosthand/integration/projection/MediaProjectionProvider.kt, app/src/main/java/com/folklore25/ghosthand/service/accessibility/GhostCoreAccessibilityService.kt, app/src/main/java/com/folklore25/ghosthand/routes/read/ReadScreenshotRouteHandlers.kt, app/src/test/java/com/folklore25/ghosthand/routes/read/ReadScreenshotRouteHandlersTest.kt, app/src/test/java/com/folklore25/ghosthand/integration/projection/MediaProjectionProviderTest.kt</files>
  <behavior>
    - Test 1: `GET /screenshot` with default size keeps native or full-resolution capture intent.
    - Test 2: projection capture waits for a real frame and fails truthfully when a frame is not acquired.
    - Test 3: route success requires non-empty image bytes, non-blank base64, and positive returned dimensions.
  </behavior>
  <action>In `MediaProjectionProvider`, replace immediate first-image acquisition with a real frame-wait path and validate that encoded PNG bytes are materially present. In `GhostCoreAccessibilityService`, treat `width=0,height=0` as native capture, guard invalid resize requests, and require successful non-empty encoding before returning success. In `ReadScreenshotRouteHandlers`, serialize success only from validated results and reject empty-image success cases outright.</action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "*ReadScreenshotRouteHandlersTest" --tests "*MediaProjectionProviderTest" --tests "*ScreenshotDispatchResultTruthTest"</automated>
  </verify>
  <done>Full-resolution screenshot is restored in the supported case, and Ghosthand no longer reports success with empty or dimensionless image output.</done>
</task>
</tasks>

<verification>
Plan 26-01 failing tests should now pass. Manual inspection is not enough; the route and backend regression tests must prove the empty-image path is gone.
</verification>

<success_criteria>
Normal `/screenshot` requests produce truthful full-resolution capture when possible, and every success response now carries a real image payload with real dimensions.
</success_criteria>

<output>
After completion, create `.planning/phases/26-visual-observation-regression-correction-for-1-4-1/26-02-SUMMARY.md`.
</output>
