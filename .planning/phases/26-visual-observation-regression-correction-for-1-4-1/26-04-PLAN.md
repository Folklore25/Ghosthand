---
phase: 26-visual-observation-regression-correction-for-1-4-1
plan: 04
type: execute
wave: 4
depends_on:
  - 26-03
files_modified:
  - app/src/main/java/com/folklore25/ghosthand/routes/read/ReadScreenshotRouteHandlers.kt
  - app/src/main/java/com/folklore25/ghosthand/catalog/GhosthandCommandCatalogRoutes.kt
  - scripts/ghosthand-verify-runtime.sh
  - app/src/test/java/com/folklore25/ghosthand/routes/read/ReadScreenshotRouteHandlersTest.kt
  - app/src/test/java/com/folklore25/ghosthand/interaction/execution/ScreenshotDispatchResultTruthTest.kt
  - app/src/test/java/com/folklore25/ghosthand/preview/ScreenPreviewCoordinatorTest.kt
  - app/src/test/java/com/folklore25/ghosthand/screen/StateCoordinatorScreenPayloadTest.kt
  - .planning/STATE.md
autonomous: true
requirements:
  - VIS-04
  - VIS-07
must_haves:
  truths:
    - "Screenshot failures distinguish materially different known causes instead of collapsing them into generic screenshot failure."
    - "Regression coverage protects route truth, preview truth, and empty-image rejection together."
    - "The phase closes only after local build plus unit tests and real-device screenshot verification pass."
  artifacts:
    - path: "app/src/main/java/com/folklore25/ghosthand/routes/read/ReadScreenshotRouteHandlers.kt"
      provides: "Bounded screenshot failure classification"
    - path: "scripts/ghosthand-verify-runtime.sh"
      provides: "Real-device screenshot verifier that rejects empty payloads and useless preview dimensions"
    - path: ".planning/STATE.md"
      provides: "Updated Phase 26 verification status after successful gates"
  key_links:
    - from: "ReadScreenshotRouteHandlers"
      to: "GhosthandCommandCatalogRoutes"
      via: "truthful screenshot failure and preview wording"
      pattern: "route output and route description stay aligned"
    - from: "runtime verifier"
      to: "/screenshot"
      via: "device check for non-empty bytes and returned dimensions"
      pattern: "real payload proof instead of prefix-only success"
---

<objective>
Tighten screenshot failure classification, complete the regression test coverage, and prove the corrected path on a real device before closing Phase 26.
</objective>

<context>
Keep this final plan bounded to screenshot-specific failure semantics, screenshot-specific command wording, focused tests, and real-device verification. Do not reopen broader observation or interface design.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Replace generic screenshot failure with bounded specific classification and aligned route wording</name>
  <files>app/src/main/java/com/folklore25/ghosthand/routes/read/ReadScreenshotRouteHandlers.kt, app/src/main/java/com/folklore25/ghosthand/catalog/GhosthandCommandCatalogRoutes.kt, app/src/test/java/com/folklore25/ghosthand/routes/read/ReadScreenshotRouteHandlersTest.kt, app/src/test/java/com/folklore25/ghosthand/interaction/execution/ScreenshotDispatchResultTruthTest.kt, app/src/test/java/com/folklore25/ghosthand/preview/ScreenPreviewCoordinatorTest.kt, app/src/test/java/com/folklore25/ghosthand/screen/StateCoordinatorScreenPayloadTest.kt</files>
  <behavior>
    - Test 1: route failures distinguish invalid requested dimensions, unavailable capability, projection session or activity loss, frame acquisition timeout, encode failure, and empty output where the runtime can know them.
    - Test 2: screenshot-related catalog wording matches the narrowed truthful route semantics without introducing new product scope.
  </behavior>
  <action>Use the validated screenshot result information from Plans 26-02 and 26-03 to map known screenshot failure causes into bounded specific route codes, while keeping the route contract operationally useful and truthful. Update `GhosthandCommandCatalogRoutes.kt` only where wording must change to match the corrected screenshot and preview behavior. Extend the route and related regression tests so the classification cannot collapse back into generic success or generic failure.</action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "*ReadScreenshotRouteHandlersTest" --tests "*StateCoordinatorScreenPayloadTest" --tests "*ScreenPreviewCoordinatorTest" --tests "*ScreenshotDispatchResultTruthTest"</automated>
  </verify>
  <done>Known screenshot failure modes are classified specifically enough to guide the caller, and the route description matches the actual bounded behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen the runtime verifier and close the phase with full local and device proof</name>
  <files>scripts/ghosthand-verify-runtime.sh, .planning/STATE.md</files>
  <action>Strengthen the screenshot verification flow in `scripts/ghosthand-verify-runtime.sh` so `screenshot-check` fails on empty payloads, blank base64 after the prefix, zero-byte decode results, and preview dimensions below the decision-usable floor established in Plan 26-03. Run the full compile plus unit-test gate and the device screenshot verifier, then update `.planning/STATE.md` only after those checks pass.</action>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin testDebugUnitTest</automated>
    <automated>scripts/ghosthand-verify-runtime.sh screenshot-check</automated>
  </verify>
  <done>Phase 26 closes with passing local verification, passing real-device screenshot proof, and updated project state.</done>
</task>
</tasks>

<verification>
The final phase gate is not complete until the real-device screenshot verifier proves non-empty image payloads and useful preview dimensions, not just route status codes.
</verification>

<success_criteria>
Ghosthand now reports screenshot success and failure truthfully, preview and OCR visual cues stay aligned with usable capture, and the corrected 1.4.1 screenshot path is proven by local tests plus device verification.
</success_criteria>

<output>
After completion, create `.planning/phases/26-visual-observation-regression-correction-for-1-4-1/26-04-SUMMARY.md`.
</output>
