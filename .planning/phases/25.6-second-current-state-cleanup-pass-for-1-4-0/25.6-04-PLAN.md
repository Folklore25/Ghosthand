---
phase: 25.6-second-current-state-cleanup-pass-for-1-4-0
plan: 04
type: execute
wave: 4
depends_on:
  - 25.6-03
files_modified:
  - .planning/STATE.md
  - app/src/main/java/com/folklore25/ghosthand/**
  - app/src/test/java/com/folklore25/ghosthand/**
autonomous: true
requirements:
  - CURFIX2-01
  - CURFIX2-05
  - CURFIX2-08
must_haves:
  truths:
    - "The pass closes only after build and unit tests pass."
    - "The final report must include direct proof that app/src/main/java has zero wildcard imports."
    - "Verification should not mutate accepted fixes or broaden scope."
---

<objective>
Finish Phase 25.6 with final proof that wildcard imports are gone and the branch still builds and tests cleanly.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Run final verification and capture zero-wildcard proof</name>
  <files>.planning/STATE.md, app/src/main/java/com/folklore25/ghosthand/**, app/src/test/java/com/folklore25/ghosthand/**</files>
  <action>Run the full compile plus unit-test gate, record direct proof that `app/src/main/java` contains zero wildcard imports, and update the phase closeout state only after the verification passes.</action>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin testDebugUnitTest</automated>
    <manual>Capture the zero-wildcard proof with a direct source scan over `app/src/main/java`.</manual>
  </verify>
  <done>The second cleanup pass is complete, verified, and ready to execute from a clean planning baseline.</done>
</task>
</tasks>
