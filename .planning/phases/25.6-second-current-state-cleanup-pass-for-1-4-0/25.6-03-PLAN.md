---
phase: 25.6-second-current-state-cleanup-pass-for-1-4-0
plan: 03
type: execute
wave: 3
depends_on:
  - 25.6-02
files_modified:
  - app/src/test/java/com/folklore25/ghosthand/**
autonomous: true
requirements:
  - CURFIX2-04
  - CURFIX2-05
  - CURFIX2-07
must_haves:
  truths:
    - "Root-flat tests should move only when domain ownership is clear."
    - "Touched tests must also lose wildcard-import residue and other obvious import clutter."
    - "Truly shared or cross-domain tests may remain in root, but only by clear justification rather than inertia."
---

<objective>
Reduce the remaining root-flat tests further and leave the touched test tree cleaner and more domain-owned.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Move clearly owned root-flat tests and clean import hygiene in touched tests</name>
  <files>app/src/test/java/com/folklore25/ghosthand/**</files>
  <action>Move the remaining root-flat tests into `ui/main`, `ui/permissions`, `ui/diagnostics`, `ui/common/dialog`, or other owner packages wherever the ownership is obvious, keep only clearly shared or cross-domain tests at root, and clean wildcard or self-package import residue in every touched test file while preserving test behavior.</action>
  <verify>
    <manual>Confirm that any tests left in `app/src/test/java/com/folklore25/ghosthand` are clearly shared or cross-domain rather than just unmoved leftovers.</manual>
  </verify>
  <done>The root-flat test set is reduced further and the touched tests have cleaner import hygiene.</done>
</task>
</tasks>
