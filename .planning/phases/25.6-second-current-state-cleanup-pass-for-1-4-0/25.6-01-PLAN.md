---
phase: 25.6-second-current-state-cleanup-pass-for-1-4-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/folklore25/ghosthand/ui/main/**
  - app/src/main/java/com/folklore25/ghosthand/ui/permissions/**
  - app/src/main/java/com/folklore25/ghosthand/ui/diagnostics/**
  - app/src/main/java/com/folklore25/ghosthand/ui/common/dialog/**
  - app/src/main/java/com/folklore25/ghosthand/ui/common/model/**
autonomous: true
requirements:
  - CURFIX2-01
  - CURFIX2-06
  - CURFIX2-07
must_haves:
  truths:
    - "The remaining wildcard-import cleanup must finish at zero wildcard imports in app/src/main/java, not just a reduced count."
    - "The cleanup is concentrated in 16 UI-owned files and must preserve UI behavior exactly."
    - "Self-package wildcard imports must also be removed rather than preserved as a shortcut."
---

<objective>
Remove the remaining UI-owned wildcard imports and bring main source to zero wildcard imports without changing behavior.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Replace the remaining UI wildcard imports with explicit imports</name>
  <files>app/src/main/java/com/folklore25/ghosthand/ui/main/**, app/src/main/java/com/folklore25/ghosthand/ui/permissions/**, app/src/main/java/com/folklore25/ghosthand/ui/diagnostics/**, app/src/main/java/com/folklore25/ghosthand/ui/common/dialog/**, app/src/main/java/com/folklore25/ghosthand/ui/common/model/**</files>
  <action>Remove every remaining wildcard import from the 16 UI-owned main-source files, replace them with only the explicit imports actually required, remove any self-package star imports entirely, and keep runtime or UI behavior unchanged.</action>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin</automated>
    <manual>Prove zero wildcard imports remain with `python3 - <<'PY'\nfrom pathlib import Path\ncount = 0\nfiles = 0\nfor path in Path('app/src/main/java').rglob('*.kt'):\n    local = sum(1 for line in path.read_text().splitlines() if line.startswith('import ') and line.rstrip().endswith('.*'))\n    if local:\n        files += 1\n        count += local\nprint(files)\nprint(count)\nPY`.</manual>
  </verify>
  <done>Main source contains zero wildcard imports.</done>
</task>
</tasks>
