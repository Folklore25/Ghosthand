# 25.6-03 Summary

- Moved clearly owned UI tests into `ui/main`, `ui/permissions`, `ui/common/dialog`, and `ui/diagnostics`.
- Reduced root-flat tests from 9 to 4.
- Cleaned import hygiene in the touched tests, including the maintainability/package-ownership test.

Verification:
- `./gradlew :app:compileDebugUnitTestKotlin`
