---
phase: 10-agent-integration-scenario-hardening
task: 2
total_tasks: 3
status: in_progress
last_updated: 2026-03-28T13:26:49.853Z
---

<current_state>
Phase 10 is active and Phase 09 remains frozen. Stage 1 is complete via the `/commands` canonical contract hardening. Stage 2 is in progress and now has two on-device scenario runners implemented: Scenario 01 (`scenario-settings-search-back`) and the real Scenario 02 (`scenario-settings-home-screenshot`). Both passed on-device. The current working tree only contains the uncommitted runner and workflow-doc updates for these scenario additions.
</current_state>

<completed_work>

- Task 1: Agent contract usability - Done
  - committed as `14931fc` (`test: harden commands canonical contract`)
- Task 2: Scenario hardening - In progress
  - Scenario 01 added and accepted:
    - `scenario-settings-search-back`
    - verifies Settings search -> result click -> back with `/wait` confirmation
  - Scenario 02 corrected and accepted:
    - `scenario-settings-home-screenshot`
    - verifies Settings baseline -> `/home` + `/wait` -> launcher screenshot confirmation

</completed_work>

<remaining_work>

- Task 2: Scenario hardening
  - commit the current scenario-runner and workflow-doc updates
  - implement Scenario 03 only:
    - `notification read -> navigation`
- Task 3: On-device operator integration
  - not started
  - should layer OpenClaw -> GhostEye -> Ghosthand validation on top of the already-proven device-shell baseline

</remaining_work>

<decisions_made>

- Keep Phase 09 accepted and frozen; do not continue polishing accepted endpoints unless a real regression appears
- Build Phase 10 scenarios first on low-variance Settings/system surfaces before moving to noisier flows
- Treat screenshot confirmation in Scenario 02 as valid only after `/wait` confirms the launcher transition
- Use Ghosthand-native scenario runners as the first scenario-hardening layer before adding operator-path validation

</decisions_made>

<blockers>
- No current runtime blocker is open from Scenario 01 or Scenario 02
- Operational caveat remains: on this ROM, runtime restore can require the app-owned runtime start control before `/ping` becomes available
- Current local delta is intentionally uncommitted:
  - `scripts/ghosthand-verify-runtime.sh`
  - `docs/Runtime-Verification-Workflow.md`
</blockers>

<context>
This is a Phase 10 hardening pause point, not a Phase 09 pause point. Do not go back into route creation or Phase 09 polish. Resume by making the current scenario-runner/doc delta deliberate, then continue with Scenario 03 only. Keep validation scenario-level and bounded.
</context>

<next_action>
1. Review the uncommitted Scenario 01/02 runner changes and the workflow-doc update
2. Commit that Phase 10 Stage 2 scenario slice narrowly
3. Implement Scenario 03 (`notification read -> navigation`) only
</next_action>
