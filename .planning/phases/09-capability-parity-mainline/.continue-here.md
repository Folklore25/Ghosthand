---
phase: 09-capability-parity-mainline
task: 3
total_tasks: 5
status: in_progress
last_updated: 2026-03-27T22:18:53.164Z
---

<current_state>
Phase 09 is in runtime-acceptance correction, not final closeout. The route surface is broadly aligned and the APK builds, but acceptance is still open on the phone. Major routes already verified on-device include `/ping`, `/screen`, `/tree`, `/info`, `/find`, `/tap`, `/click`, `/back`, `/home`, `/recents`, `/clipboard`, `/notify`, and `/commands`. Current pause point is screenshot and wait semantics, plus final verification of the remaining text and gesture routes.
</current_state>

<completed_work>

- Added runtime-acceptance planning addendum: `RUNTIME-ACCEPTANCE-PLAN.md`
- Reworked Ghosthand route semantics toward the settled local-agent contract without copying external code or naming
- Added fresh snapshot and snapshot-scoped node identity work:
  - current-window tree selection
  - path-based `nodeId`
  - stale-reference handling
- Added action-ready read surfaces:
  - `/screen` returns filtered UI elements with `centerX` / `centerY`
  - `/find` accepts selector-style input and returns action-ready geometry
  - `/commands` exposes route/method/param metadata for local agents
- Added notification buffer support and `/notify` read path
- Made local API request handling concurrent so blocking `/wait` calls no longer starve the server
- Added crash-safe screenshot handling and secondary capture paths
- Root-installed and live-verified on-device:
  - `GET /ping`
  - `GET /screen`
  - `GET /tree`
  - `GET /info`
  - `POST /find`
  - `POST /tap`
  - `POST /click`
  - `POST /back`
  - `POST /home`
  - `POST /recents`
  - `GET/POST /clipboard`
  - `GET/POST /notify`
  - `GET /commands`

</completed_work>

<remaining_work>

- Finish `/screenshot` runtime acceptance
  - current state: no app crash, but still returns `SCREENSHOT_FAILED`
  - root `screencap` works directly under `su -c`
  - MediaProjection button click no longer crashes the app, but actual permission completion still needs validation
- Re-run `/wait` against a real foreground transition after the latest semantic fix
- Verify remaining routes live on-device:
  - `/focused`
  - `/input`
  - `/setText`
  - `/scroll`
  - `/swipe`
  - `/longpress`
  - `/gesture`
- Commit code changes only after the remaining runtime blockers are understood or reduced

</remaining_work>

<decisions_made>

- Keep Ghosthand naming and architecture, but adapt the external MVP's method: fresh current-window reads, action-ready responses, and explicit stale-reference handling
- Add `GET /commands` so on-device agents can discover Ghosthand's commands directly from the app
- Make the local API server concurrent because single-threaded request handling made `/wait` unusable in practice
- Keep screenshot handling crash-safe and fall through to fallback capture paths instead of letting unsupported platform calls kill the process

</decisions_made>

<blockers>
- `/screenshot`: still unavailable on-device
  - direct AccessibilityService screenshot capability is denied on this device
  - MediaProjection consent path no longer crashes, but successful grant/capture not yet confirmed
  - root screencap works directly, but the in-process fallback path still needs to return actual route data
- `/wait`: semantic verification still not proven with a passing live Settings -> Home transition
- Repeated APK reinstalls can leave Ghosthand accessibility enabled but not rebound until secure settings are rewritten using separate `su -c settings put secure ...` commands
</blockers>

<context>
This is no longer broad surface-building work. The route set is mostly there and many routes are already live-verified. Resume by treating the phone as the source of truth: use root to start the service, rebind Ghosthand accessibility after installs, then continue with screenshot first, wait second, and the remaining text/gesture endpoints after that.
</context>

<next_action>
1. Restore runtime after resume:
   - start Ghosthand foreground service with `su -c am start-foreground-service ...`
   - rewrite secure accessibility settings to rebind Ghosthand core
2. Finish `/screenshot`:
   - either complete MediaProjection consent successfully
   - or make the in-process root fallback return route data
3. Re-run `/wait` against a real Settings -> Home foreground change
4. Verify `/focused`, `/input`, `/setText`, `/scroll`, `/swipe`, `/longpress`, `/gesture`
</next_action>
