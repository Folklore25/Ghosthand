---
phase: 09-capability-parity-mainline
task: 2
total_tasks: 3
status: in_progress
last_updated: 2026-03-28T11:37:44.381Z
---

<current_state>
Phase 09 route creation is done and the verified route set is runtime accepted. Current work is post-acceptance non-feature hardening only. The committed baseline already includes the Android implementation, runtime acceptance record, contract extraction, pure payload/selector/wait/HTTP helpers, JVM tests, and the first runtime verification runner. The current working tree contains the next runner refinement plus local doc-boundary correction.
</current_state>

<completed_work>

- Committed the Android/Gradle implementation baseline:
  - `286a5ac` — `chore: add ghosthand android implementation baseline`
- Committed planning refresh and acceptance record:
  - `84d103e` — `docs: refresh phase-09 runtime acceptance state`
- Refreshed the runtime contract to a usable local-agent surface:
  - fresh current-window snapshots
  - snapshot-scoped node IDs
  - action-ready `/screen` and `/find`
  - selector-driven `/click`
  - machine-readable `GET /commands`
  - concurrent local API request handling
- Added non-feature hardening commits:
  - `adf14ef` — contract/payload/selector/wait hardening
  - `2b32c8a` — stronger runtime contract coverage
  - `3d1b487` — base runtime verification script
  - `b2fb78b` — verification workflow doc
  - `e9adb2d` — focused-input and selector-click checks
  - `18b8c08` — screenshot and notify verification checks
- Verified on-device via device-shell orchestration:
  - `GET /ping`
  - `GET /screen`
  - `GET /tree`
  - `GET /info`
  - `GET /focused`
  - `POST /find`
  - `POST /tap`
  - `POST /click` by `text`, `desc`, and `id`
  - `POST /input`
  - `POST /setText`
  - `POST /scroll`
  - `POST /swipe`
  - `POST /longpress`
  - `POST /gesture`
  - `POST /back`
  - `POST /home`
  - `POST /recents`
  - `GET /screenshot`
  - `GET/POST /notify`
  - `GET/POST /clipboard`
  - `GET /wait`
  - `GET /commands`
- Committed non-feature hardening around runtime contract and verification:
  - `adf14ef` — contract/payload/selector/wait hardening
  - `2b32c8a` — stronger runtime contract coverage
  - `3d1b487` — base runtime verification script
  - `b2fb78b` — runtime verification workflow doc
  - `e9adb2d` — focused-input and selector-click checks
  - `18b8c08` — screenshot and notify verification checks

</completed_work>

<remaining_work>

- Finish task 2 — runtime verification runner hardening:
  - decide whether to keep and commit the local `commands-schema-check` / `tree-find-click` / smoke-core-full refinements
  - keep the runner focused on meaningful chain behavior and first-failing-step output
- Then handle task 3 — doc boundary correction:
  - keep `docs/ghosthanddev/` reference-only
  - keep Ghosthand progression in `.planning/` and project-owned docs
  - decide which current local docs should remain local-only vs be committed later

</remaining_work>

<decisions_made>

- Keep Ghosthand naming and architecture, but adapt the external MVP's method: fresh current-window reads, action-ready responses, and explicit stale-reference handling
- Add `GET /commands` so on-device agents can discover Ghosthand's commands directly from the app
- Make the local API server concurrent because single-threaded request handling made `/wait` unusable in practice
- The settled screenshot baseline must not require manual root grant or manual screenshot permission grant from users
- Use device-shell orchestration as the authoritative acceptance method for the local-agent route set
- Keep `docs/ghosthanddev/` reference-only; Ghosthand project progression belongs in `.planning/` and Ghosthand-owned docs

</decisions_made>

<blockers>
- No route-surface blocker currently prevents ongoing use or acceptance of the verified route set
- Main caveat: repeated APK reinstalls on this ROM can require explicit accessibility rebind through secure settings during testing
- Main technical debt: regression protection is still too light relative to the now-verified route contract
- Local docs and runner updates are partly updated but intentionally uncommitted right now
</blockers>

<context>
This is a hardening pause point, not an implementation pause point. Do not go back into route creation. Resume by inspecting the current local-only changes and deciding whether to capture the next runner hardening increment in git before taking on more work.
</context>

<next_action>
1. Review `scripts/ghosthand-verify-runtime.sh` local changes and either commit or discard the new runner refinements
2. Keep `docs/ghosthanddev/` reference-only and decide what to do with the current local doc-boundary corrections
3. Continue non-feature hardening only after the current local delta is made deliberate
</next_action>
