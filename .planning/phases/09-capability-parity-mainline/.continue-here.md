---
phase: 09-capability-parity-mainline
task: 7
total_tasks: 7
status: paused
last_updated: 2026-03-27T19:16:34.687Z
---

<current_state>
Stages 1 and 2 of the 3-stage capability-parity program are complete and building cleanly. Stage 3 (final stage) has not been started. All 12 new endpoints from Stages 1+2 compile and the APK assembles successfully.
</current_state>

<completed_work>

- Stage 1 — 6 endpoints: /screen, /info, /focused, /click, /input, /setText
  - GhostAccessibilityExecutionCore: added takeScreenshot, setTextOnNode, ScreenshotDispatchResult, NodeTextDispatchResult
  - GhostCoreAccessibilityService: full takeScreenshot (gesture callback + bitmap build), setTextOnNode
  - GhostAccessibilityService (legacy): stub implementations
  - AccessibilityClicker: new — node click wrapper delegating to performNodeClick
  - AccessibilityTyper: added setTextOnNode() + SetTextAttemptResult
  - StateCoordinator: new orchestrator methods for all 6 endpoints
  - LocalApiServer: 6 new route handlers
  - docs/API.md: sections 7.16–7.21

- Stage 2 — 6 endpoints: /scroll, /longpress, /gesture, /back, /home, /recents
  - GhostAccessibilityExecutionCore: added GestureStroke, GesturePoint, GlobalActionResult types
  - GhostCoreAccessibilityService: performLongPressGesture, performGesture (multi-stroke), doGlobalAction
  - GhostAccessibilityService (legacy): same three methods
  - AccessibilityScroller: new — computes swipe vectors from node bounds (35% fraction, 300ms)
  - StateCoordinator: scrollNode, performGlobalAction (via when-cast to concrete service), performLongPressGesture, performGesture
  - LocalApiServer: 6 new routes (scroll, longpress, gesture, back, home, recents) via shared buildGlobalActionResponse helper
  - docs/API.md: sections 7.22–7.27
</completed_work>

<remaining_work>

- Stage 3 — 4 endpoints:
  - /screenshot: MediaProjection API for full-screen capture (separate permission, not accessibility)
  - /notify: NotificationListenerService for notification dispatch/cancel
  - /wait: tree polling with condition matching (uses AccessibilityTreeSnapshotProvider + AccessibilityNodeFinder)
  - /clipboard: ClipboardManager read/write
  All four require new Android capabilities beyond the current scope.
</remaining_work>

<decisions_made>

- performGlobalAction is final on AccessibilityService — cannot be in GhostAccessibilityExecutionCore interface
  Solved with doGlobalAction() on concrete services called via when(service) cast in StateCoordinator
- GLOBAL_ACTION_BACK/HOME/RECENTS all use shared buildGlobalActionResponse with actionCode parameter
- GestureStroke/GesturePoint live in GhostAccessibilityExecutionCore.kt as shared data classes
- /scroll uses node bounds center + 35% fractional swipe, 300ms duration
- /longpress defaults to 500ms, range 100–10000ms
- /gesture accepts array of strokes each with points[] and durationMs
</decisions_made>

<blockers>
- None on Stages 1+2
- Stage 3 requires new Android permissions: MediaProjection (user consent dialog), NotificationListenerService (user enable), no new permission for ClipboardManager
</blockers>

<context>
The capability-parity plan is at .planning/phases/09-capability-parity-mainline/CAPABILITY-PARITY-PLAN.md
Key architectural note: GhostAccessibilityExecutionCoreRegistry.currentInstance() returns the connected primary service (GhostCoreAccessibilityService). Both concrete services must implement all interface methods and the doGlobalAction() pattern.
The interface cannot include performGlobalAction because AccessibilityService.performGlobalAction is final.
</context>

<next_action>
Start Stage 3: implement /screenshot (MediaProjection), /notify (NotificationListenerService), /wait (tree polling), /clipboard (ClipboardManager). Read CAPABILITY-PARITY-PLAN.md Stage 3 section first.
</next_action>
