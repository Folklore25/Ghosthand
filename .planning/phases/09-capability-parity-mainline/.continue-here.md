---
phase: 09-capability-parity-mainline
task: 1
total_tasks: 3
status: paused
last_updated: 2026-03-27T18:45:02.445Z
---

<current_state>
Paused at the start of the new highest-priority mainline task: produce a staged capability-parity implementation plan only. The user wants Ghosthand to stay accessibility-first, keep root optional and secondary, and port proven external accessibility implementation patterns where appropriate without using the external project name in code, comments, strings, or docs.
</current_state>

<completed_work>

- Dark premium UI refinement: Done
  - Switched the home surface to a near-black theme with yellow accent emphasis.
  - Removed the foreground-package row from the default visible operator surface.
  - Rebuilt, installed, and visually verified the updated UI on-device.
- Task 1: Current parity audit - In progress
  - Read the required product/docs/knowledgebase files.
  - Confirmed existing Ghosthand capability baseline:
    - `/tree`
    - `/find`
    - `/tap`
    - `/swipe`
    - `/type`
  - Confirmed current reusable integration points:
    - `LocalApiServer`
    - `StateCoordinator`
    - `GhostCoreAccessibilityService`
    - `AccessibilityTreeSnapshotProvider`
    - `AccessibilityNodeFinder`
    - `AccessibilityTapper`
    - `AccessibilitySwiper`
    - `AccessibilityTyper`
</completed_work>

<remaining_work>

- Task 1: Finish the parity-gap list
  - Compare the requested target capability set against what Ghosthand already exposes.
- Task 2: Write the staged implementation plan
  - Stage 1:
    - `/screen`
    - `/info`
    - `/focused`
    - `/click`
    - `/input`
    - `/setText`
  - Stage 2:
    - `/scroll`
    - `/longpress`
    - `/gesture`
    - `/back`
    - `/home`
    - `/recents`
  - Stage 3:
    - `/screenshot`
    - `/notify`
    - `/wait`
    - `/clipboard`
  - For each stage include:
    - files likely to change
    - Ghosthand code already reusable
    - what exact implementation strategy can be ported/adapted
    - target-device acceptance path
- Task 3: Return the planning-only report
  - current phase
  - current parity gap list
  - staged implementation plan
  - which existing Ghosthand components can be reused
  - first stage to execute next
</remaining_work>

<decisions_made>

- Ghosthand remains accessibility-first; root stays optional and secondary.
- The parity program should port/adapt proven external accessibility patterns instead of re-deriving the mechanics.
- This task is planning-only. Do not start implementing the new capability set until the staged plan is delivered.
- The user also mentioned service-button toggle behavior and small alignment issues in the UI, but those are separate from the planning-only parity task.
</decisions_made>

<blockers>
- No technical blocker on the planning task. The interruption happened before the final staged report was written.
- Working tree contains uncommitted UI refinement changes from the completed dark-theme pass.
</blockers>

<context>
Useful audit findings already gathered:

- `LocalApiServer` currently handles:
  - `/health`
  - `/state`
  - `/device`
  - `/foreground`
  - `/tree`
  - `/find`
  - `/tap`
  - `/swipe`
  - `/type`
  - `/launch`
  - `/stop`
- `docs/API.md` documents `/screenshot`, but there is no implementation in `LocalApiServer` yet.
- `GhostCoreAccessibilityService` already has reusable low-level action primitives:
  - `performTapGesture`
  - `performSwipeGesture`
  - `performSwipeGestureDiagnostic`
  - `performNodeClick`
  - `performSetText`
  - focused editable-node resolution
- `AccessibilityNodeFinder` already supports `focused` strategy on the flat tree snapshot.
- `AccessibilityTreeSnapshotProvider` already exposes package/activity plus flat nodes, which is the obvious reuse base for `/screen`, `/info`, and `/focused`.
- The latest live UI verification showed the dark premium home surface is working, so the next mainline effort should return to capability parity instead of more UI drift.
</context>

<next_action>
Start with: write the five-part planning report from the gathered evidence. The first concrete recommendation should be to execute Stage 1 next, because it reuses the most existing Ghosthand code and closes the read/basic-action parity gap before broader gestures, global actions, and utility features.
</next_action>
