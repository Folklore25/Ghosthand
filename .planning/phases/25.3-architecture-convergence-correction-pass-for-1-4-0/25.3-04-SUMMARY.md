# 25.3-04 Summary

- `/commands` now publishes `capabilityIds` plus canonical capability definition projections instead of only raw capability IDs.
- `/state.permissions.capabilitySummary` now projects the fuller runtime availability model with blockers, required services or permissions, and current backend or mode where relevant.
- Kept `/commands`, `/state`, and `/capabilities` aligned to the same canonical capability source.

Verification:
- `GhosthandCommandCatalogTest`
- `StatePayloadComposerTest`
- `StateCoordinatorStatePayloadTest`
