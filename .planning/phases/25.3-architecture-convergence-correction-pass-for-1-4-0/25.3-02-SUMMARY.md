# 25.3-02 Summary

- Moved the remaining root-owned UI, service, interaction, screen, runtime-state, and HTTP helper files into domain directories.
- Reduced the root source directory to a single Kotlin file: `GhosthandApp.kt`.
- Updated ownership tests and file-path based contract tests to follow the new directory layout.

Verification:
- `find app/src/main/java/com/folklore25/ghosthand -maxdepth 1 -type f -name '*.kt'` now returns only `GhosthandApp.kt`.
- `./gradlew :app:compileDebugKotlin`
