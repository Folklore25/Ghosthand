---
phase: 25.3-architecture-convergence-correction-pass-for-1-4-0
plan: 03
type: execute
wave: 3
depends_on:
  - 25.3-01
files_modified:
  - app/src/main/java/com/folklore25/ghosthand/capability/**
  - app/src/main/java/com/folklore25/ghosthand/routes/system/**
  - app/src/main/java/com/folklore25/ghosthand/server/**
  - app/src/main/java/com/folklore25/ghosthand/state/**
  - app/src/main/java/com/folklore25/ghosthand/catalog/**
autonomous: true
requirements:
  - ACFIX-03
  - ACFIX-04
  - ACFIX-07
must_haves:
  truths:
    - "Capability Plane v2 must exist as a first-class model, not as a few extra fields on route metadata."
    - "`GET /capabilities` is mandatory and must be capability-centric rather than route-centric."
    - "Dynamic capability availability must be derived from real runtime state instead of static duplication."
---

<objective>
Implement the canonical Capability Plane v2 model and the first-class `/capabilities` surface.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement Capability Plane v2 and add `GET /capabilities`</name>
  <files>app/src/main/java/com/folklore25/ghosthand/capability/**, app/src/main/java/com/folklore25/ghosthand/routes/system/**, app/src/main/java/com/folklore25/ghosthand/server/**, app/src/main/java/com/folklore25/ghosthand/state/**, app/src/main/java/com/folklore25/ghosthand/catalog/**</files>
  <action>Implement canonical CapabilityDefinition and CapabilityAvailability models backed by one capability source of truth, add a CapabilityPresentation layer that drives external surfaces, and wire a real capability-centric `GET /capabilities` route that exposes definitions, availability, blockers, preconditions, failure modes, directness, truth type, and route mapping in a compact machine-readable form.</action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests com.folklore25.ghosthand.capability.CapabilityPlaneTest --tests com.folklore25.ghosthand.routes.CapabilitiesRouteHandlersTest :app:compileDebugKotlin</automated>
  </verify>
  <done>Capability Plane v2 is first-class in code and `/capabilities` exists as the capability-centric surface.</done>
</task>
</tasks>
