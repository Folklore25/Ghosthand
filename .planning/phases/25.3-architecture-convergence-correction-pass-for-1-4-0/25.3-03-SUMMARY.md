# 25.3-03 Summary

- Kept the first-class `/capabilities` surface in place.
- Strengthened the canonical capability-plane implementation by exposing reusable command capability projections from the same capability definitions used by `/capabilities`.
- Preserved the canonical `CapabilityDefinition` and `CapabilityAvailability` model as the source of truth.

Verification:
- `CapabilityPlaneTest`
- `CapabilitiesRouteHandlersTest`
