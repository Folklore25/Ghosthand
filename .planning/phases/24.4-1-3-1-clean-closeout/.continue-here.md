---
phase: 24.4-1-3-1-clean-closeout
task: 4
total_tasks: 4
status: paused
last_updated: 2026-04-04T09:34:56.958Z
---

<current_state>
Phase 24.4 itself is complete on `main`, and the 1.3.1 closeout branch has already been merged and pushed.

The only live work left is post-phase release housekeeping on `main`:
- `app/build.gradle.kts` is modified but uncommitted
- it changes the version to `1.3.1 (6)`
- it permanently drops `x86` and `x86_64` by setting `ndk.abiFilters` to `arm64-v8a` and `armeabi-v7a`

The release APK was rebuilt successfully after that change and the file size dropped from about `57 MB` to about `34 MB`.
</current_state>

<completed_work>

- Task 1: Preview cleanup and real retrieval path — Done
- Task 2: Field/hint convergence and noise reduction — Done
- Task 3: Project/code cleanup — Done
- Task 4: Final runtime/catalog closeout and full verification — Done
- Post-phase release investigation: confirmed the APK was large mostly because ML Kit OCR native libs were bundled for four ABIs — Done
- Post-phase release optimization: added arm-only ABI filters and rebuilt release successfully — Done, but not committed yet
</completed_work>

<remaining_work>

- Commit `app/build.gradle.kts` on `main`
- Push the new `1.3.1 (6)` + arm-only packaging change to `origin/main`
- Optionally install the rebuilt release APK to the OnePlus device if needed
</remaining_work>

<decisions_made>

- Preview now uses `previewPath` and `/screenshot` instead of inline `/screen` preview bytes because that is clearer and lighter.
- 1.3.1 closeout contract alignment stayed bounded to runtime/catalog because `docs/API.md` is absent in this workspace.
- The release should permanently drop `x86` and `x86_64`; only `arm64-v8a` and `armeabi-v7a` should ship.
- The final release version must be `1.3.1 (6)`.
</decisions_made>

<blockers>
- No technical blocker.
- The only outstanding work is the uncommitted release-housekeeping change on `main`.
</blockers>

<context>
This is not another feature or refactor phase.
The meaningful product/code work is already done.

What remains is straightforward release hygiene:
- commit the Gradle version/ABI change
- push `main`
- optionally reinstall the fresh APK

Do not reopen 24.4 scope while resuming unless new user input changes the task.
</context>

<next_action>
Start with:
1. `git add app/build.gradle.kts`
2. `git commit -m "chore(release): bump to 1.3.1 and ship arm-only APK" --no-verify`
3. `git push origin main`

Then, if requested, install [app-release.apk](/Users/zohar/Code/Project/ongoing/Ghosthand/app/build/outputs/apk/release/app-release.apk) to the OnePlus device.
</next_action>
