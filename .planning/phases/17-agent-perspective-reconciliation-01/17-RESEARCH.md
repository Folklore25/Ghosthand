# Phase 17 Research — Agent Perspective Reconciliation 01

## What already counts as settled truth

- `/wait` is already part of accepted operator-path use and runbook truth.
- Wrapper-driven `/click` already exposes reconciliation metadata such as matched-node vs wrapper resolution.
- Selector friction is already narrowed relative to the earlier broad clickable-gate interpretation.

## What the exploratory run can still legitimately tell us

- Which capabilities are hard to discover naturally from a zero-context agent perspective.
- Where current structured truth may still under-express the next action an agent should reasonably infer.
- Which parts of the failure belong to Android/app constraints rather than Ghosthand defects.

## Honest planning implication

The phase should not implement broad code changes first. It should:
1. preserve the exploratory run in repo truth
2. classify each issue
3. correct false conclusions
4. narrow the next platform-owned direction

That is enough value for one bounded phase.
