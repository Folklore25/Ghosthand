# Phase 17 PRD — Agent Perspective Reconciliation 01

## Goal

Convert the latest zero-context OpenClaw exploratory evaluation into truthful repo follow-up work without letting exploratory misunderstanding overwrite accepted platform truth.

## Scope

- Preserve the exploratory run as useful agent-perspective evidence.
- Explicitly correct false missing-capability claims.
- Split reported issues into:
  - real substrate defects
  - discoverability / natural-consumption weaknesses
  - app/platform constraints
  - agent misclassification / rejected conclusions
- Define the next platform-owned direction only after classification.

## Out of Scope

- No runtime rewrite.
- No skill-repo implementation.
- No broad roadmap expansion.
- No acceptance of exploratory endpoint-missing claims that contradict accepted repo truth.

## Required Corrections

- `/wait` must not be described as missing.
- Wrapper-driven `/click` must not be regressed back to “opaque by default”.
- Selector friction must remain interpreted as a narrower discoverability/selector-surface problem rather than a broad platform-opacity claim.
