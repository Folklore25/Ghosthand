---
phase: 25.5-current-state-correction-pass-for-1-4-0
plan: 02
type: execute
wave: 2
depends_on:
  - 25.5-01
files_modified:
  - app/src/main/AndroidManifest.xml
  - app/src/main/java/com/folklore25/ghosthand/service/runtime/GhosthandForegroundService.kt
  - app/src/main/java/com/folklore25/ghosthand/integration/projection/MediaProjectionProvider.kt
  - app/src/main/java/com/folklore25/ghosthand/capability/**
  - app/src/test/java/com/folklore25/ghosthand/**
autonomous: true
requirements:
  - CURFIX-02
  - CURFIX-06
  - CURFIX-07
must_haves:
  truths:
    - "Manifest declarations, runtime startForeground flags, provider comments, and capability claims must converge on one truthful screenshot or MediaProjection contract."
    - "The current permission flow and runtime model stay intact; this pass aligns truth and declarations rather than redesigning consent UX."
    - "A lightweight contract guard should make service-type drift harder to reintroduce."
---

<objective>
Align the screenshot or MediaProjection foreground-service contract so Ghosthand’s declared, documented, and runtime behavior all describe the same supported path.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Converge manifest, runtime flags, provider documentation, and capability truth for screenshot capture</name>
  <files>app/src/main/AndroidManifest.xml, app/src/main/java/com/folklore25/ghosthand/service/runtime/GhosthandForegroundService.kt, app/src/main/java/com/folklore25/ghosthand/integration/projection/MediaProjectionProvider.kt, app/src/main/java/com/folklore25/ghosthand/capability/**, app/src/test/java/com/folklore25/ghosthand/**</files>
  <action>Update the manifest permissions and foreground-service declaration, align `GhosthandForegroundService.startForeground(...)` service-type flags with the manifest, tighten MediaProjection provider comments to the same contract, and add or update a contract-sensitive test that catches future drift between the screenshot capability claim and the foreground-service setup.</action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "*Projection*" --tests "*ForegroundService*"</automated>
  </verify>
  <done>Screenshot capability truth, provider guidance, manifest declarations, and runtime service flags are internally consistent and regression-guarded.</done>
</task>
</tasks>
