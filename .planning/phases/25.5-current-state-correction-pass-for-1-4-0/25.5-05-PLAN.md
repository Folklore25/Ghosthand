---
phase: 25.5-current-state-correction-pass-for-1-4-0
plan: 05
type: execute
wave: 5
depends_on:
  - 25.5-04
files_modified:
  - app/src/test/java/com/folklore25/ghosthand/**
  - .planning/STATE.md
autonomous: true
requirements:
  - CURFIX-05
  - CURFIX-07
  - CURFIX-08
must_haves:
  truths:
    - "Test moves should follow clear domain ownership and should not create noisy churn outside the touched correction areas."
    - "Regression coverage must specifically protect notification truthfulness and screenshot-service contract alignment."
    - "The pass closes only after the full compile plus unit-test gate passes."
---

<objective>
Finish Phase 25.5 with continued test-package convergence, focused regression protection, and the final verification gate.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Move root-flat tests to owner packages, update maintainability checks, and run the final gate</name>
  <files>app/src/test/java/com/folklore25/ghosthand/**, .planning/STATE.md</files>
  <action>Move the remaining root-flat tests into domain-owned test packages wherever ownership is obvious, keep only truly shared helpers in shared locations, update any maintainability or ownership checks that enforce test-package convergence, add or update only the focused regression tests needed for notification truthfulness and screenshot or foreground-service contract alignment, and then run the final compile plus unit-test gate.</action>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin testDebugUnitTest</automated>
  </verify>
  <done>Test ownership is more converged than before, the new correction risks are covered, and the project still compiles with unit tests passing.</done>
</task>
</tasks>
