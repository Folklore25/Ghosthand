---
phase: 25.5-current-state-correction-pass-for-1-4-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/folklore25/ghosthand/notification/NotificationDispatcher.kt
  - app/src/main/java/com/folklore25/ghosthand/notification/NotificationBuffer.kt
  - app/src/main/java/com/folklore25/ghosthand/state/StateCoordinator.kt
  - app/src/main/java/com/folklore25/ghosthand/routes/system/SystemRouteHandlers.kt
  - app/src/test/java/com/folklore25/ghosthand/**
autonomous: true
requirements:
  - CURFIX-01
  - CURFIX-06
  - CURFIX-07
must_haves:
  truths:
    - "Notification cancel behavior must become symmetric with tagged notification post behavior without widening the external `/notify` contract."
    - "NotificationBuffer entries must only be removed when the actual recorded notification identity matches the cancellation identity."
    - "The implementation must stay truthful if cancellation cannot resolve a matching tag or recorded notification."
---

<objective>
Make `/notify` cancellation truthful for tagged notifications and lock the behavior with focused regression coverage before other cleanup work begins.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Resolve exact notification identity during cancel and protect the route with focused tests</name>
  <files>app/src/main/java/com/folklore25/ghosthand/notification/NotificationDispatcher.kt, app/src/main/java/com/folklore25/ghosthand/notification/NotificationBuffer.kt, app/src/main/java/com/folklore25/ghosthand/state/StateCoordinator.kt, app/src/main/java/com/folklore25/ghosthand/routes/system/SystemRouteHandlers.kt, app/src/test/java/com/folklore25/ghosthand/**</files>
  <action>Refactor notification cancel handling so it resolves the correct recorded notification identity internally, cancels with the exact tag-plus-id pair when needed, keeps the `/notify` route contract backward-compatible, and updates or adds focused tests that prove POST then DELETE by returned `notificationId` removes the correct tagged notification while mismatched identities do not silently mutate buffer state.</action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "*Notification*"</automated>
  </verify>
  <done>`/notify` no longer claims success for tagged notifications that remain posted, and the buffer reflects only truthful removal.</done>
</task>
</tasks>
