---
phase: 25.5-current-state-correction-pass-for-1-4-0
plan: 03
type: execute
wave: 3
depends_on:
  - 25.5-02
files_modified:
  - app/src/main/java/com/folklore25/ghosthand/**/*.kt
autonomous: true
requirements:
  - CURFIX-03
  - CURFIX-06
must_haves:
  truths:
    - "Wildcard-import cleanup is mandatory maintainability work and must remove self-package and project-wide star imports from main Kotlin source."
    - "Cleanup should preserve behavior and rely on explicit imports or same-package visibility rather than stylistic churn."
    - "The main source tree must compile cleanly after the cleanup and zero wildcard imports should remain."
---

<objective>
Remove wildcard-import pollution from main source so package ownership is legible and the accepted 1.4.0 structure is actually readable.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Replace star imports across main Kotlin source with explicit imports or same-package references</name>
  <files>app/src/main/java/com/folklore25/ghosthand/**/*.kt</files>
  <action>Remove all wildcard imports from `app/src/main/java`, including nonsensical self-package star imports and broad cross-project imports, replacing them with explicit imports where needed and relying on same-package visibility where imports are unnecessary, while keeping runtime behavior unchanged.</action>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin</automated>
    <manual>Confirm that `rg -n '^import .*\\*$' app/src/main/java --glob '*.kt'` returns no results.</manual>
  </verify>
  <done>Main Kotlin source no longer hides ownership behind wildcard imports and the app still compiles.</done>
</task>
</tasks>
