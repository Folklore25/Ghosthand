# 25.5-01 Summary

- Fixed `/notify` cancellation to resolve the exact recorded tag-plus-id identity before cancellation.
- Kept the external `/notify` contract backward-compatible while making buffer removal truthful.
- Added focused notification regression coverage for successful tagged cancellation, missing recorded identity, and tag-mismatch buffer retention.

Verification:
- `./gradlew :app:testDebugUnitTest --tests "*Notification*"`
