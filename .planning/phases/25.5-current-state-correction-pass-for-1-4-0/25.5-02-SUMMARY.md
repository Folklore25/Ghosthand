# 25.5-02 Summary

- Aligned the screenshot or MediaProjection foreground-service contract across manifest permission declarations, service type declarations, runtime `startForeground(...)` flags, and provider documentation.
- Added a contract guard test so service-type drift is caught in unit tests.

Verification:
- `./gradlew :app:testDebugUnitTest --tests "*ForegroundService*" --tests "*Projection*"`
